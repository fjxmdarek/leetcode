# 53. 最大子数组和 (Maximum Subarray)

### 题目描述

给你一个整数数组 `nums` ，请你找出一个具有最大和的连续子数组（子数组最少包含一个元素），返回其最大和。

**子数组** 是数组中的一个连续部分。

### 示例

**示例 1：**

> **输入：** nums = [-2,1,-3,4,-1,2,1,-5,4]
> **输出：** 6
> **解释：** 连续子数组 [4,-1,2,1] 的和最大，为 6 。

**示例 2：**

> **输入：** nums = [1]
> **输出：** 1

**示例 3：**

> **输入：** nums = [5,4,-1,7,8]
> **输出：** 23

### 提示

* `1 <= nums.length <= 10^5`
* `-10^4 <= nums[i] <= 10^4`

### 进阶

如果你已经实现复杂度为 $O(n)$ 的解法，尝试使用更为精妙的 **分治法** 求解。

---

### 解题思路（动态规划 / Kadane 算法）

这是解决此题最经典且高效的方法。

1. **状态定义**：

   - 设 `dp[i]` 是以第 `i` 个元素 **结尾** 的连续子数组的最大和。
2. **转移方程**：

   - 对于第 `i` 个元素，我们有两种选择：
     - 将 `nums[i]` 加入到前面的子数组中：`dp[i-1] + nums[i]`
     - 另起炉灶，从 `nums[i]` 开始重新计算：`nums[i]`
   - 因此：`dp[i] = max(nums[i], dp[i-1] + nums[i])`
   - 简单来说：如果前面的和是负数，那就丢掉它，从当前数重新开始。
3. **空间优化**：

   - 由于 `dp[i]` 只与 `dp[i-1]` 有关，我们不需要数组，只需一个变量 `pre` 来记录上一个状态，以及一个 `max_ans` 记录全局最大值。

---

### 复杂度分析

* **时间复杂度**：$O(n)$。只需遍历一遍数组。
* **空间复杂度**：$O(
