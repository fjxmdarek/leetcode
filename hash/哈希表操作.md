# 查找

```
auto it = hashtable.find(target - nums[i]);//需要返回值的查找；
if(!num_set.count(num-1))；//不需要返回值（判断有无）
```

# 开头结尾

```
for (auto it = mp.begin(); it != mp.end(); ++it) 
```

# 取元素

```
return {it->second, i};
```

# 放入元素

```
for (const int& num : nums){num_set.insert(num);}
```
